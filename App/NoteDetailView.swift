import SwiftUI
import SkillsManagerCore

/// Task 10 stub — replaced by the full note detail view.
struct NoteDetailView: View {
    let file: NoteFile

    var body: some View {
        Text(file.name)
    }
}
